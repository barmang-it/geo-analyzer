
import { BusinessClassification } from '../types';

export const classifyMajorBrand = (fullText: string): BusinessClassification | null => {
  // Remove hardcoded company checks - let the intelligent system handle all companies
  return null;
};
